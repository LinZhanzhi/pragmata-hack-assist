# YOLO detection dataset: detect the puzzle panel (bbox only).
# Paths are relative to this file.

path: D:/pathSolver/panel_detector/dataset
train: images/train
val: images/val
test: images/test

names:
  0: puzzle_panel
